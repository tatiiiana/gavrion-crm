import {NextResponse} from "next/server";
import {requireSitePermission} from "@/lib/site-access";
import {sanitizeSiteConfig} from "@/lib/site-builder";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params,access=await requireSitePermission("sites.read",id);if(!access)return NextResponse.json({error:"Acceso denegado."},{status:403});const {data,error}=await access.admin.from("site_project_versions").select("id,version,configuration,created_at,created_by").eq("site_project_id",id).order("version",{ascending:false}).limit(30);return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json(data||[])}
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params,access=await requireSitePermission("sites.write",id);if(!access)return NextResponse.json({error:"Acceso denegado."},{status:403});const body=await request.json();const {data:version,error}=await access.admin.from("site_project_versions").select("configuration,version").eq("site_project_id",id).eq("id",String(body.versionId||"")).maybeSingle();if(error||!version)return NextResponse.json({error:error?.message||"La versión no existe."},{status:404});const configuration=sanitizeSiteConfig(version.configuration);const {data:updated,error:updateError}=await access.admin.from("site_projects").update({configuration,status:"draft"}).eq("id",id).select().single();if(updateError)return NextResponse.json({error:updateError.message},{status:400});return NextResponse.json({...updated,restoredVersion:version.version})}
